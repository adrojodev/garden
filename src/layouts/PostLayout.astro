---
import Analytics from '@vercel/analytics/astro'

const { frontmatter } = Astro.props;

const hideFooter = frontmatter.hideFooter

import FloweDivider from "../components/FloweDivider.astro";
import "../styles/global.css"
import "../styles/posts.css"
---
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/png" href="/favicon.png" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{frontmatter.title}</title>
	</head>
	<body class="bg-[#F1F0E9] flex justify-center px-4 py-8">
		<main class="flex flex-col gap-4 w-full max-w-5xl">
			<header>
				<a href="/">← Home</a>
			</header>
			<section class="flex flex-col gap-2">
				<h1>{frontmatter.title}</h1>
				<div class="flex flex-col">
					<p>👟 Publicado: <i>{frontmatter.publishedDate}</i></p>
					<p>🏁 Última edición: <i>{frontmatter.lastEditionDate}</i></p>
				</div>
			</section>
			<hr />
            <slot />
			{!hideFooter && (
				<div class="flex flex-col gap-3">
					<p>
						Pues nada, si te gustó este post y te interesa conectar conmigo sientete libre de mandarme mensaje en instagram <a target="_blank" href="https://www.instagram.com/ad_rojo/">@ad_rojo</a>. Y revisa el <a href="/">home</a> ahí no solo están los posts, también pongo links que me van interesando de cosas chidas que voy encontrando por ahí.
					</p>
					<p>
						Muchas gracias por leer ✌️ bye ✨
					</p>
				</div>
				<div class="flex flex-col w-full gap-1">
					<FloweDivider />
					<span class="w-full flex justify-center items-center text-center text-sm">Garden, 2025</span>
				</div>
			)}
		</main>
		<Analytics />
	</body>
</html>